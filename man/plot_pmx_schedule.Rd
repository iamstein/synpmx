% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{plot_pmx_schedule}
\alias{plot_pmx_schedule}
\title{Draw a cohort's dosing and observation schedule}
\usage{
plot_pmx_schedule(
  data,
  roles,
  coarsen_time = TRUE,
  max_patients = 80L,
  main = NULL
)
}
\arguments{
\item{data}{A PMX dataset -- normally the source.}

\item{roles}{Explicit roles from \code{\link[=pmx_roles]{pmx_roles()}}.}

\item{coarsen_time}{Draw the coarsened visit grid (\code{TRUE}, the default) or
the recorded times as given (\code{FALSE}).}

\item{max_patients}{Draw at most this many patients, evenly spread through
the ordering so the shape of the cohort survives. Default 80.}

\item{main}{Plot title. Defaults to a description of what is drawn.}
}
\value{
The \code{\link[=skeleton_uniqueness]{skeleton_uniqueness()}} table for the drawn data, invisibly.
}
\description{
The picture behind \code{\link[=skeleton_uniqueness]{skeleton_uniqueness()}}. One row per patient, one mark
per event: when they were dosed, and when each endpoint was observed. Read
it to decide whether a uniqueness count is a real problem or ordinary
dropout.
}
\details{
Two panels:
\itemize{
\item \strong{the map} -- patients ordered by how long they were followed, so dropout
reads as a staircase down the right-hand edge. A patient whose observation
schedule no other patient shares is marked in the margin, and their label
is drawn in red.
\item \strong{the visit histogram} -- how many patients were observed at each time on
the grid. A protocol grid gives tall bars at a handful of times. A bar of
height one is a moment only one patient was sampled at, which is precisely
what \code{\link[=synpmx_avatar]{synpmx_avatar()}} would copy verbatim onto an avatar, and those bars
are drawn in red.
}

By default the times are \strong{coarsened first}, so the picture shows the grid
\code{\link[=synpmx_avatar]{synpmx_avatar()}} actually generates on. Pass \code{coarsen_time = FALSE} to see
the recorded times instead; drawing it both ways is the quickest way to see
what coarsening bought.

Source-derived, like every diagnostic here: keep the figure inside the safe
environment.
}
\examples{
data <- pmx_simulated_fixture(20)
roles <- pmx_roles(
  id = "ID", time = "TIME", dv = "DV", amt = "AMT", evid = "EVID",
  cmt = "CMT", dvid = "DVID", covariates = "WT"
)
plot_pmx_schedule(data, roles)
}
\seealso{
\code{\link[=skeleton_uniqueness]{skeleton_uniqueness()}}, \code{\link[=synpmx_avatar]{synpmx_avatar()}}.
}
