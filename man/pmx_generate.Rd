% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_structural.R
\name{pmx_generate}
\alias{pmx_generate}
\title{Generate a mock PMX event table from a structural model}
\usage{
pmx_generate(
  x,
  design = NULL,
  n_subjects = NULL,
  seed = NULL,
  dropout = 0,
  lloq = NULL
)
}
\arguments{
\item{x}{A \code{pmx_structural_model} or a \code{pmx_calibrated_model}.}

\item{design}{A \code{\link[=pmx_trial_design]{pmx_trial_design()}}. Taken from \code{x} when it is a calibrated
model.}

\item{n_subjects}{Number of subjects. Defaults to the planned cohort total,
or to the released private count for a calibrated model.}

\item{seed}{Ordinary generation seed. Unrelated to privacy noise.}

\item{dropout}{Fraction of subjects who discontinue early. A public
assumption from the protocol.}

\item{lloq}{Lower limit of quantification. Observations below it are flagged
\code{CENS = 1} with \code{DV} at the limit, following the Monolix convention.}
}
\value{
A data frame in PMX event-table form.
}
\description{
Works in two modes. Supplied a \code{\link[=pmx_structural_model]{pmx_structural_model()}} it generates purely
from public inputs, reads no confidential data, and makes no privacy claim.
Supplied a \code{\link[=fit_calibrated_pmx]{fit_calibrated_pmx()}} result it uses the privately corrected
parameters; that is post-processing and consumes no further budget.
}
