% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pca.R
\name{pmx_pca_dosing}
\alias{pmx_pca_dosing}
\title{The dosing model each arm was generated from}
\usage{
pmx_pca_dosing(x)
}
\arguments{
\item{x}{A dataset from \code{\link[=synpmx_pca]{synpmx_pca()}}, or its model.}
}
\value{
A data frame with \code{arm}, \code{dose}, \code{time}, \code{amt}, \code{share},
\code{patients} and \code{distinct}.
}
\description{
One row per arm and dose, giving the time and the amount. This is the
schedule the arm holds in common: the modal set of times and amounts among
its patients, never an individual's. \code{share} is the fraction of the arm
holding it, so a schedule picked by a bare plurality is visible as one, and
\code{distinct} is how many schedules that arm actually contained.
}
\details{
A study recording its dose times as actuals rather than as planned times
holds close to one schedule per patient. The generated data then holds one
per arm, which is the mechanism working and a real loss of variety.
}
\examples{
data <- pmx_simulated_fixture(60)
roles <- pmx_roles(
  id = "ID", time = "TIME", nominal_time = "NTIME", dv = "DV", amt = "AMT",
  evid = "EVID", cmt = "CMT", dvid = "DVID", mdv = "MDV"
)
pmx_pca_dosing(synpmx_pca(data, roles, seed = 1))
}
\seealso{
\code{\link[=synpmx_pca]{synpmx_pca()}}, \code{\link[=pmx_pca_visits]{pmx_pca_visits()}}, \code{\link[=pmx_pca_report]{pmx_pca_report()}}.
}
