% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/endpoint-types.R
\name{pmx_endpoint_types}
\alias{pmx_endpoint_types}
\title{What kind of values each endpoint takes}
\usage{
pmx_endpoint_types(data, roles)
}
\arguments{
\item{data}{PMX data.}

\item{roles}{Explicit roles from \code{\link[=pmx_roles]{pmx_roles()}}.}
}
\value{
A \code{pmx_endpoint_types} data frame with one row per endpoint and
columns \code{endpoint}, \code{type}, \code{levels}, \code{decided_by}, and \code{reason}. Marked
\code{"restricted_not_releasable"}: the level set is read from real data.
}
\description{
Reports, per endpoint, whether its values are continuous or discrete, and
where that answer came from. \code{\link[=synpmx_avatar]{synpmx_avatar()}} blends real trajectories, and
a weighted mean of several patients' zeros and ones is a number between them,
so a discrete endpoint would come back continuous unless the generated values
are snapped back onto the levels the source used. This is the function that
decides which endpoints that applies to.
}
\details{
An endpoint is called:
\itemize{
\item \code{"binary"} when every observed value is 0 or 1;
\item \code{"ordinal"} when every observed value is a whole number and there are at
most 12 distinct ones, which are then the scale;
\item \code{"integer"} when every observed value is a whole number and there are more
levels than that, so generated values are rounded rather than snapped onto
a scale. Counts are the usual case, and \code{"count"} may be used to declare
one, but the evidence is only that the values are whole numbers;
\item \code{"continuous"} otherwise, including when there are fewer than 10 observed
values, which is too few to call.
}

Declare \code{endpoint_types} in \code{\link[=pmx_roles]{pmx_roles()}} to override any of it. The \code{reason}
column says what the data showed, so a study whose endpoint was called wrongly
can be corrected without guessing at the rule.

Snapping a binary or ordinal endpoint means generated values are source
values: a 0/1 endpoint has no third value to emit. What protects a patient on
a discrete endpoint is the visit-set and dose-schedule machinery, not the
distinctness of any one number.
}
\examples{
data <- pmx_simulated_fixture(30)
roles <- pmx_roles(
  id = "ID", time = "TIME", dv = "DV", amt = "AMT", evid = "EVID",
  cmt = "CMT", dvid = "DVID", covariates = "WT"
)
pmx_endpoint_types(data, roles)
}
\seealso{
\code{\link[=pmx_roles]{pmx_roles()}}, \code{\link[=synpmx_avatar]{synpmx_avatar()}}, \code{\link[=synpmx_scorecard]{synpmx_scorecard()}}.
}
