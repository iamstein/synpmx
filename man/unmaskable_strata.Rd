% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{unmaskable_strata}
\alias{unmaskable_strata}
\title{Which strata can mask their own avatars}
\usage{
unmaskable_strata(data, roles, min_pattern_share = 2L, coarsen_time = TRUE)
}
\arguments{
\item{data}{A PMX dataset -- the source, before generating.}

\item{roles}{Explicit roles from \code{\link[=pmx_roles]{pmx_roles()}}.}

\item{min_pattern_share}{The floor \code{\link[=synpmx_avatar]{synpmx_avatar()}} will be run with: how
many patients must share a pattern before it may be reused.}

\item{coarsen_time}{Score the coarsened visit grid (\code{TRUE}, the default, and
what \code{\link[=synpmx_avatar]{synpmx_avatar()}} does) or the recorded times as given.}
}
\value{
A \code{pmx_unmaskable_strata} data frame, worst arm first, with
\code{stratum}, \code{patients}, \code{unmaskable_dosing}, \code{unmaskable_visits} and
\code{safe_anchors}. One row named \code{all} when the roles declare no strata.
}
\description{
One row per declared stratum level, counting the patients in it that no
avatar can safely be built on. An avatar is anchored on one real patient and
copies that patient's dose times verbatim, so an arm has to contain somebody
who can be masked; re-anchoring picks a different anchor, but never one from
another arm, because the anchor carries its \code{strata} values into the output
and moving it would silently rewrite the arm sizes.
}
\details{
\code{safe_anchors} is therefore the number that matters. An arm with none of them
will emit avatars carrying a real patient's schedule whatever the generator
does, which is what \code{\link[=synpmx_scorecard]{synpmx_scorecard()}} reports as B1a and B1b, and the fix
is to the study description rather than to the run -- see the alert
\code{\link[=synpmx_avatar]{synpmx_avatar()}} raises.

Two ways a patient cannot be masked, counted separately because the remedies
differ:
\itemize{
\item \code{unmaskable_dosing} -- their set of dose times is shared by fewer than
\code{min_pattern_share} patients, and no prefix of it can be given to an avatar
either: every shorter opening is one that either a single patient stopped
at, or that fewer than \code{min_pattern_share} patients passed through. Dose
events are copied from the anchor as they stand, since resampling them
would emit regimens the protocol never permitted, so there is nothing to
substitute.
\item \code{unmaskable_visits} -- the set of visits they have observations at is
shared by fewer than \code{min_pattern_share} patients, and their schedule group
holds no set that is shared widely enough to put in its place. Usually
fixable: a wider pool, or \code{strata = NULL} so the arms share one.
}

This reads real patient data and is marked \code{"restricted_not_releasable"}.
}
\examples{
data <- pmx_simulated_fixture(20)
data$ARM <- ifelse(as.integer(data$ID) \%\% 2L == 0L, "A", "B")
roles <- pmx_roles(
  id = "ID", time = "TIME", dv = "DV", amt = "AMT", evid = "EVID",
  cmt = "CMT", dvid = "DVID", covariates = "WT", strata = "ARM"
)
unmaskable_strata(data, roles)
}
\seealso{
\code{\link[=skeleton_uniqueness]{skeleton_uniqueness()}} for the same question per patient,
\code{\link[=synpmx_scorecard]{synpmx_scorecard()}}, which reports this as rows B1a and B1b.
}
