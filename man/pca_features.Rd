% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pca.R
\name{pca_features}
\alias{pca_features}
\title{Every feature the components are built on}
\usage{
pca_features(x)
}
\arguments{
\item{x}{A dataset from \code{\link[=synpmx_pca]{synpmx_pca()}}, or its trial summary.}
}
\value{
A data frame with \code{feature}, \code{kind}, \code{endpoint}, \code{time}, \code{covariate},
\code{level}, \code{patients}, \code{center}, \code{scale} and \code{transform}. Marked
\code{"restricted_not_releasable"}: the counts and moments are read from real
data.
}
\description{
One row per column of the matrix \code{synpmx_pca_summarize()} decomposed: one
per baseline covariate, and one per endpoint per retained nominal time. This
is the grid the whole method sits on, so it is where to look first when a
generated dataset is missing a visit or a covariate.
}
\details{
\code{center} and \code{scale} are the column's mean and standard deviation on the
modelling scale, which is the log scale for a positive endpoint --- see
\code{transform} in the same row. \code{patients} is how many subjects hold an
observation in that cell; cells held by fewer than \code{min_column_patients} were
dropped rather than modelled, so they do not appear here at all.
}
\examples{
data <- pmx_simulated_fixture(60)
roles <- pmx_roles(
  id = "ID", time = "TIME", nominal_time = "NTIME", dv = "DV", amt = "AMT",
  evid = "EVID", cmt = "CMT", dvid = "DVID", mdv = "MDV"
)
head(pca_features(synpmx_pca_summarize(data, roles)))
}
\seealso{
\code{\link[=synpmx_pca_summarize]{synpmx_pca_summarize()}}, \code{\link[=pca_components]{pca_components()}}, \code{\link[=pca_report]{pca_report()}}.
}
