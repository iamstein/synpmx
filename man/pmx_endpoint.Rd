% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/endpoints.R
\name{pmx_endpoint}
\alias{pmx_endpoint}
\title{Declare endpoint scientific-clock behavior}
\usage{
pmx_endpoint(
  dvid = NULL,
  alignment,
  transform = c("auto", "log", "identity"),
  shape,
  units = NULL,
  grid = NULL,
  cmt = NULL,
  subject_sd = 0.2,
  residual_sd = 0.08,
  censoring = NULL
)
}
\arguments{
\item{dvid}{Public DVID value for this endpoint, or \code{NULL} when no DVID
column is used.}

\item{alignment}{One of \code{"dose_relative"}, \code{"study_time"}, \code{"occasion"},
or \code{"hybrid"}.}

\item{transform}{One of \code{"log"}, \code{"identity"}, or \code{"auto"}. \code{"auto"} uses
only the public DV bounds: a nonnegative domain uses an offset log scale.}

\item{shape}{One of \code{"occasion"} or \code{"global"}; this is a broad public shape
expectation, not a fitted structural model.}

\item{units}{Optional public unit label.}

\item{grid}{Optional strictly increasing public grid on the declared clock.}

\item{cmt}{Optional public observation compartment value.}

\item{subject_sd, residual_sd}{Public generation variability multipliers.}

\item{censoring}{Optional public list with \code{left}, \code{right}, or a two-value
\code{interval}. Source-dependent censoring frequencies are separately private.}
}
\value{
A \code{pmx_endpoint} declaration.
}
\description{
Endpoint alignment is public scientific metadata. It controls how a small
fixed-dimensional trajectory summary is built and how new trajectories are
generated; it is not a PK or PD model.
}
