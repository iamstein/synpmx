% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/structural.R
\name{pmx_trial_design}
\alias{pmx_trial_design}
\title{Declare a public trial design}
\usage{
pmx_trial_design(
  dose_levels = NULL,
  cohort_sizes = NULL,
  sampling,
  n_doses = 1L,
  dose_interval = 24,
  dose_escalation = NULL,
  dose_times = NULL,
  duration = 0,
  visit_window = 0.05,
  routes = NULL,
  source
)
}
\arguments{
\item{dose_levels}{Dose amounts, one per cohort. Omit when using
\code{dose_escalation}.}

\item{cohort_sizes}{Planned subjects per cohort, recycled over \code{dose_levels}.
Defaults to equal cohorts.}

\item{sampling}{Nominal sampling times after a dose, from the protocol,
measured from that dose. A single numeric vector applies the same schedule
after every dose. Real protocols often do not: to sample richly after some
doses and sparsely or not at all after others, supply a \strong{list with one
element per dose}, using \code{NULL} for a dose with no samples. A seven-dose
study with full profiles on the first and last days and a single trough in
between is
\code{sampling = list(rich, 0, NULL, NULL, NULL, 0, rich)}.}

\item{n_doses}{Number of doses per subject in a parallel design.}

\item{dose_interval}{Time between doses when doses are equally spaced.}

\item{dose_escalation}{Per-occasion dose amounts for a within-subject
escalation, for example \code{c(10, 30, 100)}. Applied to every subject. For a
trial that also escalates between cohorts, supply a list of sequences, one
per cohort, for example \code{list(c(1, 2, 4), c(2, 4, 8), c(4, 8, 16))}, with
\code{cohort_sizes} giving the subjects in each. Every sequence must have the
same length, since the dosing schedule is shared.}

\item{dose_times}{Explicit dose times, for example \code{c(0, 7, 14)}. Defaults to
equally spaced times at \code{dose_interval}.}

\item{duration}{Infusion duration; zero for bolus or oral.}

\item{visit_window}{Fractional jitter applied to nominal times.}

\item{routes}{Administration route per dose, \code{"iv"} or \code{"extravascular"},
for a study dosed by more than one. Required by the \code{"1cmt_mixed"} and
\code{"2cmt_mixed"} structural models, which resolve to a single-route form per
dose and apply the model's \code{f} to the extravascular ones only; an
intravenous dose carries no bioavailability term because it is all of the
dose. One vector serves every cohort, so an intravenous loading dose
followed by subcutaneous maintenance is
\code{c("iv", "extravascular", "extravascular")}. A list gives each cohort its
own, the way \code{dose_escalation} does, which is how a study with an
intravenous arm, a subcutaneous arm and an arm that switches is written
down. A single entry is recycled over the doses.}

\item{source}{Required provenance string.}
}
\value{
A \code{pmx_trial_design}.
}
\description{
Every field is a design fact from the protocol and consumes no privacy
budget. See the data elicitation guide at
\url{https://iamstein.github.io/synpmx/articles/data-elicitation.html} for which
parts of a realized design need a provenance note.
}
\details{
Two dosing patterns are supported. A parallel design gives each cohort one
dose level (\code{dose_levels}) repeated \code{n_doses} times. A within-subject
escalation gives every subject the same increasing sequence of doses
(\code{dose_escalation}), one per occasion; this is prespecified design, not an
outcome, when the escalation follows a fixed protocol schedule.

A study that doses by more than one route declares \code{routes} alongside
either pattern. Without it a mixed-route structural model is refused rather
than generated as though every dose were intravenous.
}
