% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covariates.R
\name{pmx_covariates_reference}
\alias{pmx_covariates_reference}
\title{Reference distributions for the usual baseline covariates}
\usage{
pmx_covariates_reference(
  names,
  medians = NULL,
  source = paste("synpmx reference distribution for a",
    "broad adult population; a package", "default, not measured from any study")
)
}
\arguments{
\item{names}{Covariate column names. A plain vector takes each name as the
quantity, so \code{c("WT", "AGE")} is body weight and age. A named vector maps
a column to a quantity, so \code{c(BWT = "WT")} puts the weight distribution in
a column called \code{BWT}. A handful of common spellings resolve on their own:
\code{WEIGHTB}, \code{WEIGHT}, \code{WGT} and \code{BW} to \code{WT}, \code{HGT} and \code{HEIGHT} to \code{HT},
\code{GENDER} to \code{SEX}, \code{CLCR} to \code{CRCL}.}

\item{medians}{Optional named numeric replacing the reference median for
those columns, in the units listed by \code{\link[=pmx_covariate_reference_table]{pmx_covariate_reference_table()}}.
Named by output column, so \code{c(BWT = 82)} goes with \code{names = c(BWT = "WT")}.}

\item{source}{Provenance recorded on every covariate built here. The default
says these are package reference values rather than a study's own.}
}
\value{
A \code{pmx_covariates} object.
}
\description{
Builds \code{\link[=pmx_covariate]{pmx_covariate()}} declarations for body size, age, sex and the common
renal and hepatic laboratory values, so that a public-model generator has
plausible covariate columns without each distribution being elicited by
hand. The values are round numbers for a broad adult population.
}
\section{What these numbers are, and are not}{

They are the same kind of input as the built-in structural models in
\code{\link[=pmx_structural_model]{pmx_structural_model()}}: illustrative on purpose, so that covariate-handling
code has something to run against. They are not a rendering of any
particular trial and were not measured from one.

The spread is the half that travels. Body weight holds much the same
coefficient of variation across adult populations whatever its median is, so
the shape and the CV are reusable and the median is not: an ordinary adult
cohort and a cohort selected for obesity differ in the median alone.
\code{medians} is how to move it, and a regression test holds the reference
median against the adult studies in this package's own surveys.

Age, sex and race are not population constants. The protocol's inclusion
criteria and where it enrolled decide them, and a phase 1 healthy-volunteer
cohort, a renal-impairment study and an oncology trial have nothing in
common here. The entries put a column in the data. State your own if
anything downstream reads it. No dataset in this package carries a race
column at all.

Nothing here fits a neonatal or paediatric cohort, where body size differs
from an adult by more than an order of magnitude. Declare those with
\code{\link[=pmx_covariate]{pmx_covariate()}}.

Before generated data crosses a trust boundary, replace these with the
protocol's own criteria or a published description of the population, and
record that in each covariate's \code{source}.
}

\examples{
pmx_covariates_reference(c("WT", "AGE", "SEX"))

# A heavier cohort, in a column named the way the study names it.
pmx_covariates_reference(c(WEIGHTB = "WT"), medians = c(WEIGHTB = 117))

# `WEIGHTB` resolves on its own, so the mapping is only needed to rename.
pmx_covariates_reference("WEIGHTB")
}
\seealso{
\code{\link[=pmx_covariate_reference_table]{pmx_covariate_reference_table()}} for what is available,
\code{\link[=pmx_covariate]{pmx_covariate()}} to state one by hand,
\code{\link[=pmx_covariates_auto]{pmx_covariates_auto()}} to resample from the data instead.
}
