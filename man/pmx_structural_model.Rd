% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/structural.R
\name{pmx_structural_model}
\alias{pmx_structural_model}
\title{Declare a public structural model}
\usage{
pmx_structural_model(
  pk,
  typical,
  pd = "none",
  source,
  rx = NULL,
  iiv = c(cl = 0.3, v = 0.2),
  residual_cv = 0.15
)
}
\arguments{
\item{pk}{One of \code{"1cmt_iv"}, \code{"1cmt_oral"}, \code{"1cmt_infusion"},
\code{"2cmt_iv"}, \code{"2cmt_oral"}.}

\item{typical}{Named numeric vector of typical parameter values, interpreted
as the median of a lognormal population. Requires \code{cl} and \code{v} (the central
volume), plus \code{ka} for oral models and \code{q} and \code{v2} for two-compartment
models. Optional \code{f} defaults to 1. PD shapes additionally require
\code{baseline}, plus \code{slope} for \code{"linear"} and \code{plateau} and \code{rate} for
\code{"exponential"}.}

\item{pd}{The PD time course, with no exposure dependence. One of \code{"none"}
(PK only), \code{"constant"}, \code{"linear"} (needs \code{slope}), or \code{"exponential"}
(needs \code{plateau} and \code{rate}, covering both decay and growth). A simple
shape is adequate for exercising longitudinal code and calibrates through a
well-conditioned level correction; see \code{design/PROTOTYPE_SPEC.md}.}

\item{source}{Required provenance string recording where the model and its
typical values came from. Recorded in the release ledger.}

\item{rx}{Optional \code{rxode2} model used in place of the built-in analytic
solution. Requires the \code{rxode2} package.}

\item{iiv}{Named vector of between-subject variability, as CV on the log
scale. A public assumption; it consumes no privacy budget.}

\item{residual_cv}{Proportional residual error, as a CV.}
}
\value{
A \code{pmx_structural_model}.
}
\description{
The structural model and its typical parameter values are public inputs:
they must be established without inspecting the confidential dataset. For a
first-in-human compound they normally come from preclinical allometric
scaling, which is also what selected the starting dose.
}
