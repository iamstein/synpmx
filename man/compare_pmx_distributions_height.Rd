% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{compare_pmx_distributions_height}
\alias{compare_pmx_distributions_height}
\title{How tall the distribution figure should be drawn}
\usage{
compare_pmx_distributions_height(source, roles, per_row = 2.1, minimum = 3)
}
\arguments{
\item{source}{Source PMX data.}

\item{roles}{Explicit roles from \code{\link[=pmx_roles]{pmx_roles()}}.}

\item{per_row}{Inches per row of panels.}

\item{minimum}{Inches below which the figure is never drawn, so a
single-panel study does not come out as a letterbox.}
}
\value{
One number, in inches.
}
\description{
The figure gets one panel per endpoint and per baseline covariate, so a study
with five endpoints needs several times the height of a study with one.
\code{fig.height} is fixed before a chunk runs and cannot be read off the plot, so
pass this to the chunk option instead of letting every figure inherit one
default and arrive squashed or stranded in white space.
}
\details{
\if{html}{\out{<div class="sourceCode">}}\preformatted{```\{r, fig.height = compare_pmx_distributions_height(raw, roles)\}
compare_pmx_distributions(raw, synthetic, roles)
```
}\if{html}{\out{</div>}}
}
\examples{
data <- pmx_simulated_fixture(20)
roles <- pmx_roles(
  id = "ID", time = "TIME", dv = "DV", amt = "AMT", evid = "EVID",
  cmt = "CMT", dvid = "DVID", covariates = c("WT", "SEX")
)
compare_pmx_distributions_height(data, roles)
}
\seealso{
\code{\link[=compare_pmx_distributions]{compare_pmx_distributions()}}.
}
