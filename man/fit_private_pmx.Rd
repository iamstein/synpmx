% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{fit_private_pmx}
\alias{fit_private_pmx}
\title{Fit a subject-level differentially private PMX population generator}
\usage{
fit_private_pmx(
  data,
  roles,
  endpoints,
  epsilon,
  delta,
  bounds,
  public_design,
  contribution_limits,
  budget_allocation,
  delta_justification = NULL,
  backend = "opendp",
  public_source = FALSE
)
}
\arguments{
\item{data}{Confidential PMX event data (or a public fixture when
\code{public_source = TRUE}).}

\item{roles}{Explicit column roles from \code{\link[=pmx_roles]{pmx_roles()}}.}

\item{endpoints}{Named endpoint declarations from \code{\link[=pmx_endpoint]{pmx_endpoint()}}.}

\item{epsilon, delta}{Explicit requested subject-level privacy parameters.}

\item{bounds}{Public clipping domains from \code{\link[=pmx_bounds]{pmx_bounds()}}.}

\item{public_design}{Public schema and protocol metadata from
\code{\link[=pmx_public_design]{pmx_public_design()}}.}

\item{contribution_limits}{Public contribution limits from
\code{\link[=pmx_contribution_limits]{pmx_contribution_limits()}}.}

\item{budget_allocation}{Explicit fractions from \code{\link[=pmx_budget_allocation]{pmx_budget_allocation()}}.}

\item{delta_justification}{Required justification when \code{delta > 0}.}

\item{backend}{Production fitting supports \code{"opendp"}. \code{"public"} is a
noiseless structural backend allowed only with \code{public_source = TRUE} for
public examples; it makes no privacy claim.}

\item{public_source}{Logical assertion that the complete input is already
public. Never set this for confidential or patient data.}
}
\value{
A \code{private_pmx_model}. It contains public configuration, noisy
population summaries, privacy accounting, and one release-ledger entry;
it contains no raw IDs, rows, profiles, templates, or residuals.
}
\description{
This is the only stage that reads source patient data. It deterministically
bounds each complete subject contribution, invokes a validated DP backend for
every released source-dependent summary, and returns no raw patient records.
There is deliberately no fitting seed.
}
