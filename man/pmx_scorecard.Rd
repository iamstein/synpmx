% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scorecard.R
\name{pmx_scorecard}
\alias{pmx_scorecard}
\title{The scorecard for one synthetic dataset}
\usage{
pmx_scorecard(source, synthetic, roles, proximity = NULL)
}
\arguments{
\item{source}{Source PMX data.}

\item{synthetic}{Generated synthetic PMX data from \code{\link[=synpmx_avatar]{synpmx_avatar()}},
carrying its \code{"pmx_settings"} attribute.}

\item{roles}{Explicit roles from \code{\link[=pmx_roles]{pmx_roles()}}.}

\item{proximity}{An already-computed \code{\link[=compare_pmx_proximity]{compare_pmx_proximity()}} result, to
save recomputing it. Left \code{NULL} it is computed here, which is the slowest
part of the scorecard.}
}
\value{
A \code{pmx_scorecard} data frame with columns \code{check}, \code{question},
\code{reads}, \code{result} and \code{verdict}, marked \code{"restricted_not_releasable"}.
}
\description{
Runs the checks from \code{vignette("synthetic-data-checks")} that can be run
automatically, and returns them as one table with the answer and whether that
answer passes. The vignette is the reference for what each check asks and why
its pass criterion is what it is.
}
\details{
The \code{reads} column decides where the table may go. Rows marked \code{"source"} or
\code{"both"} were computed from real patient data, so the filled-in scorecard is
itself restricted output and belongs in the environment the source lives in.
Only the \code{"synthetic"} and \code{"run report"} rows can travel with the data.

A \code{"review"} verdict is not a soft \code{"pass"}. It marks a row where no
threshold would be honest, and it has to be read.

Three checks in the vignette are absent here because no function can produce
them: C2 (dose and observation ordering), the source-side rare-level census
behind B5, and the one that matters most -- whether the pipeline that will
consume the real study runs unchanged against this output.
}
\examples{
data <- pmx_simulated_fixture(30)
roles <- pmx_roles(
  id = "ID", time = "TIME", dv = "DV", amt = "AMT", evid = "EVID",
  cmt = "CMT", dvid = "DVID", covariates = "WT"
)
synthetic <- suppressWarnings(synpmx_avatar(data, roles, seed = 1))
pmx_scorecard(data, synthetic, roles)
}
\seealso{
\code{\link[=compare_pmx]{compare_pmx()}}, \code{\link[=pmx_masking_report]{pmx_masking_report()}},
\code{vignette("synthetic-data-checks")}.
}
