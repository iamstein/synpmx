% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate.R
\name{pmx_preflight}
\alias{pmx_preflight}
\title{Check whether a private release is worth its budget, before spending it}
\usage{
pmx_preflight(priors, epsilon, n_subjects, covariates = NULL)
}
\arguments{
\item{priors}{A \code{\link[=pmx_priors]{pmx_priors()}} object.}

\item{epsilon}{The privacy budget under consideration.}

\item{n_subjects}{Number of subjects in the fit.}

\item{covariates}{Optional \code{\link[=pmx_covariates]{pmx_covariates()}}, so the reported \code{d} matches a
fit that also releases covariate summaries.}
}
\value{
A \code{pmx_preflight} report. See \code{design/PRIVACY_BACKGROUND.md}.
}
\description{
Reports \code{f = d / (epsilon * N)}, the fraction of each prior's width that
survives as noise, and the resulting fold-error. This consumes no privacy
budget and reads no data: it depends only on the configuration.
}
\details{
The fold-error is \code{exp(f * span)}, capped at the prior's half-width because
clipping prevents a release from landing outside the prior. The uncapped
form is accurate for \code{f} below roughly 0.25 and increasingly pessimistic
above it; see \code{design/FEASIBILITY.md} section 8.
}
