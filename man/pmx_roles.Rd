% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/roles.R
\name{pmx_roles}
\alias{pmx_roles}
\title{Declare pharmacometric column roles}
\usage{
pmx_roles(
  id,
  time,
  dv,
  amt = NULL,
  evid,
  cmt = NULL,
  dvid = NULL,
  mdv = NULL,
  rate = NULL,
  nominal_time = NULL,
  tad = NULL,
  occasion = NULL,
  cens = NULL,
  limit = NULL,
  addl = NULL,
  ii = NULL,
  covariates = NULL,
  subject_properties = NULL,
  dose_covariate = NULL,
  assigned_dose = NULL,
  keep = NULL,
  exclude = NULL
)
}
\arguments{
\item{id, time, dv, evid}{Required single column names for subject ID, actual
time, dependent variable, and event ID.}

\item{amt, cmt, mdv, rate}{Optional single column names for amount,
compartment, missing-DV indicator, and infusion rate.}

\item{dvid}{Endpoint-key column(s). Usually one column. A dataset that labels
the same endpoint two ways — a numeric \code{YTYPE} beside a character \code{NAME} —
may declare both, \code{dvid = c("YTYPE", "NAME")}. The first is the grouping
key; validation checks the rest are a consistent 1:1 mapping with it and
errors if they disagree, and \code{\link[=synpmx_avatar]{synpmx_avatar()}} carries all of them through.

\strong{\code{dvid} and \code{cmt} answer different questions.} \code{dvid} is which endpoint a
measurement is; \code{cmt} is which compartment a dose enters, and it is read
only on event rows. Nothing infers one from the other, so a source with
more than one endpoint needs \code{dvid} — without it every measurement is
treated as one endpoint, sharing a single value transform and a single
censoring boundary. \code{\link[=synpmx_avatar]{synpmx_avatar()}} refuses rather than let that happen
silently.

A NONMEM \code{CMT} usually does both jobs, so \strong{one column may be named as both
roles}: \code{pmx_roles(..., cmt = "CMT", dvid = "CMT")}. This is the only
permitted overlap; every other collision is an error.}

\item{nominal_time, tad, occasion}{Optional time metadata columns.}

\item{cens, limit}{Optional Monolix-style censoring indicator and other
interval-boundary columns.}

\item{addl, ii}{Optional additional-dose and interdose-interval columns.}

\item{covariates}{Baseline covariate column names, or \code{NULL}.}

\item{subject_properties}{Treatment arm, dose group, cohort — any \strong{assigned,
subject-level stratum}, as opposed to a measured characteristic, which is a
\code{covariate}. Must be constant within subject; subjects with no recorded
value are grouped as their own stratum, with a warning rather than an
error. Several columns
may be named, and their combination defines the stratum.

\code{\link[=synpmx_avatar]{synpmx_avatar()}} carries these verbatim from the subject that supplied the
event skeleton and uses the stratum to group two things that are protocol
properties rather than patient properties: the dose-to-covariate
relationship, and the pool of attendance patterns an avatar may draw from.
It is \strong{not} a blending barrier — only route of administration is (see \code{\link[=synpmx_avatar]{synpmx_avatar()}}), so donors
are still borrowed across strata to reach the donor floor.

The differential-privacy engines model the same columns jointly with the
regimen as a released category domain.}

\item{dose_covariate}{The covariate the dose is a fixed multiple of, for
weight-based or body-surface-area dosing: name the \strong{covariate}, not the
amount. Declaring it tells \code{\link[=synpmx_avatar]{synpmx_avatar()}} to recompute each avatar's
\code{amt} from the avatar's own blended covariate at the anchor's own
milligrams-per-unit, instead of copying the anchor's milligrams.

This matters twice over. An avatar's covariates are blended while its \code{amt}
would otherwise be copied verbatim, so under proportional dosing the
avatar's own mg/kg comes out wrong — every generated patient violates the
protocol it claims to follow. And a copied amount is one real patient's
real dose, which under proportional dosing discloses that patient's weight
exactly.

Left \code{NULL}, \code{\link[=synpmx_avatar]{synpmx_avatar()}} tries to \emph{infer} the relationship, and that
inference deliberately fails closed: it requires the dose-to-covariate
ratio to collapse onto a handful of protocol levels, so a study that
rounds doses to vial sizes, or escalates within a patient by ratios that
are not quite equal, is refused and the amounts are left alone. Declaring
the covariate skips inference entirely and holds each dose row's own
ratio, so \strong{intra-patient escalation is preserved exactly} — three doses
at 1, 2 and 4 mg/kg stay at 1, 2 and 4 mg/kg. The run report says which
path was taken and, when inference declined, why.}

\item{assigned_dose}{Differential-privacy engines only. A nominal
assigned-dose column reconstructed from the generated regimen.
\code{\link[=synpmx_avatar]{synpmx_avatar()}} does not use this — carry the column with \code{keep}.}

\item{keep}{Columns to carry into synthetic data verbatim, copied from the
same source subject that supplied the event skeleton, with no blending or
synthesis. This is for assigned, subject-defining values you want kept
faithful to a subject's dosing — a treatment arm, a dose group, a
randomization sequence, or a redundant endpoint label such as a character
\code{NAME} beside a numeric \code{dvid}. Because the value comes from the same
anchor as the doses, it stays coherent with them. Contrast \code{covariates},
which are \emph{blended} into new values across neighbours. A kept value is one
real subject's real value, so use it only where the source data's own
access controls and confidentiality obligations still apply.}

\item{exclude}{Differential-privacy engines only. Columns removed before
private fitting, such as direct identifiers. \code{\link[=synpmx_avatar]{synpmx_avatar()}} does not use
this — it drops every undeclared column by default, so not naming a column
is how you drop it.}
}
\value{
A \code{pmx_roles} object used by the fitting, generation, validation, and
comparison functions.
}
\description{
Column roles are explicit: \code{synpmx} does not infer critical PMX
semantics from column names. The declaration is also the complete manifest of
what survives into synthetic data. \code{\link[=synpmx_avatar]{synpmx_avatar()}} drops every column not
named by some role, so a column you forget is dropped rather than silently
copied out of a real subject. Name a column in \code{keep} to carry it through.
}
\examples{
roles <- pmx_roles(
  id = "ID", time = "TIME", dv = "DV", amt = "AMT",
  evid = "EVID", cmt = "CMT", tad = "TAD", covariates = "WT"
)

# Two columns for one endpoint, and a treatment arm carried through verbatim.
roles <- pmx_roles(
  id = "ID", time = "TIME", dv = "DV", amt = "AMT", evid = "EVID",
  dvid = c("YTYPE", "NAME"), covariates = "WT", keep = "ARM"
)
}
