% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-generate.R
\name{synpmx_model_generate}
\alias{synpmx_model_generate}
\title{Generate a synthetic PMX dataset from a fitted model}
\usage{
synpmx_model_generate(fitted_model, n_subjects = NULL, seed = NULL)
}
\arguments{
\item{fitted_model}{A \code{pmx_fitted_model} from \code{\link[=synpmx_model_estimate]{synpmx_model_estimate()}}.}

\item{n_subjects}{Number of synthetic subjects. Defaults to the source count.}

\item{seed}{Generation seed.}
}
\value{
A data frame in the source's shape, carrying the fitted model as a
\code{pmx_fitted_model} attribute.
}
\description{
Draws new subjects from a \code{\link[=synpmx_model_estimate]{synpmx_model_estimate()}} fit. This stage reads no
patient data: its arguments are the model and a subject count, so everything
about the source that reaches the output has already passed through the fit.
}
\details{
Per subject: an arm is assigned keeping the source arm shares, covariates are
drawn from the arm's covariate model, random effects from the between-subject
covariance matrix, and the dose schedule from the arm's dosing model. The
concentration is then evaluated at the visits drawn from the arm's visit
model, against the schedule that was drawn, so a reduced or skipped dose
reaches the concentrations rather than appearing only in the dosing records.
}
\seealso{
\code{\link[=synpmx_model_estimate]{synpmx_model_estimate()}}, \code{\link[=synpmx_model]{synpmx_model()}}, \code{\link[=model_report]{model_report()}}.
}
