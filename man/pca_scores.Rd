% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pca.R
\name{pca_scores}
\alias{pca_scores}
\title{The score model each arm is generated from}
\usage{
pca_scores(x)
}
\arguments{
\item{x}{A dataset from \code{\link[=synpmx_pca]{synpmx_pca()}}, or its trial summary.}
}
\value{
A data frame with \code{arm}, \code{component}, \code{mean} and \code{sd}. Marked
\code{"restricted_not_releasable"}.
}
\description{
One row per arm and retained component, giving the mean score that arm is
centred on and the residual standard deviation it is scattered by. A
generated subject's scores are their arm's \code{mean} plus a draw whose spread is
\code{sd}, so these two columns are the whole of the between-subject variability
the synthetic data will have.
}
\details{
The \code{sd} values differ by arm on purpose. An arm sitting on an assay limit is
genuinely tighter than one well above it, and giving every arm the pooled
spread would smear the low arms upward.

Both \code{dose_term} settings report the same shape. Under \code{"factor"} the mean is
the arm's own; under \code{"log"} it is the regression evaluated at that arm's
planned total dose, and \code{sd} is the shared residual.
}
\examples{
data <- pmx_simulated_fixture(60)
roles <- pmx_roles(
  id = "ID", time = "TIME", nominal_time = "NTIME", dv = "DV", amt = "AMT",
  evid = "EVID", cmt = "CMT", dvid = "DVID", mdv = "MDV"
)
pca_scores(synpmx_pca_summarize(data, roles))
}
\seealso{
\code{\link[=synpmx_pca_summarize]{synpmx_pca_summarize()}}, \code{\link[=pca_components]{pca_components()}}, \code{\link[=pca_report]{pca_report()}}.
}
