% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synpmx.R
\name{synpmx_empirical}
\alias{synpmx_empirical}
\title{Generate a dataset from a dense differentially private release}
\usage{
synpmx_empirical(
  data,
  roles,
  endpoints,
  epsilon,
  delta,
  bounds,
  public_design,
  contribution_limits,
  budget_allocation,
  n_subjects = NULL,
  seed = 123,
  n_datasets = 1L,
  delta_justification = NULL,
  backend = "opendp",
  public_source = FALSE
)
}
\arguments{
\item{data}{The confidential dataset.}

\item{roles}{A \code{\link[=pmx_roles]{pmx_roles()}} declaration for \code{data}.}

\item{endpoints}{Named list of \code{\link[=pmx_endpoint]{pmx_endpoint()}} declarations.}

\item{epsilon}{The privacy budget. A governance decision, not a default.}

\item{delta}{Additive slack in the probability bound. The implemented Laplace
releases spend none, so realized accounting reports \code{delta = 0}.}

\item{bounds}{Public clipping domains from \code{\link[=pmx_bounds]{pmx_bounds()}}.}

\item{public_design}{A \code{\link[=pmx_public_design]{pmx_public_design()}}.}

\item{contribution_limits}{A \code{\link[=pmx_contribution_limits]{pmx_contribution_limits()}}.}

\item{budget_allocation}{A \code{\link[=pmx_budget_allocation]{pmx_budget_allocation()}} splitting \code{epsilon}
across release groups.}

\item{n_subjects}{Number of subjects to generate. Defaults to the released
noisy count.}

\item{seed}{Ordinary generation seed. Unrelated to privacy noise.}

\item{n_datasets}{Number of datasets to draw from the single release. One
dataset is returned directly; several are returned as a list.}

\item{delta_justification}{Required when \code{delta > 0}.}

\item{backend}{Privacy backend. Defaults to the validated OpenDP adapter and
fails closed if it is unavailable.}

\item{public_source}{Assert that \code{data} is genuinely public. Required by, and
only meaningful for, \code{backend = "public"}, which makes no DP claim.}
}
\value{
A data frame in the source event-table schema, carrying its release
so that \code{\link[=privacy_report]{privacy_report()}} and \code{\link[=synpmx_generate]{synpmx_generate()}} can read it. A list of
such data frames when \code{n_datasets > 1}.
}
\description{
Rather than asserting the curve shape, this mode measures it: it releases
noised summaries for the subject count, event and regimen structure,
observation timing, endpoint trajectories, baseline covariates, and
censoring, then rebuilds subjects from those summaries alone.
}
\details{
It asserts less than \code{\link[=synpmx_calibrated]{synpmx_calibrated()}} but releases far more numbers, so
one epsilon is split many ways. Utility therefore collapses below a few
hundred subjects; this mode earns its keep on large pooled corpora.

\strong{This function spends privacy budget.} Calling it again spends the budget
again. To draw further datasets from the release you have already paid for,
use \code{\link[=synpmx_generate]{synpmx_generate()}} or ask for several at once with \code{n_datasets}.
}
\section{Maintenance status}{

A secondary, provided-as-is path. \code{\link[=synpmx_avatar]{synpmx_avatar()}} is the primary, maintained
method. The differentially private modes are complete and tested but not under
active development, carry known open findings, and have not been independently
privacy-audited. Use them to demonstrate the privacy/utility tradeoff, not as
a production release mechanism; a real regulated release needs specialist
review and the external OpenDP backend. Requires a session-level
acknowledgment: call \code{\link[=synpmx_enable_dp_engines]{synpmx_enable_dp_engines()}} once before this function
will run (unless \code{backend = "public"}, which makes no DP claim).
}

\seealso{
\code{\link[=synpmx_generate]{synpmx_generate()}} to draw more datasets for free,
\code{\link[=privacy_report]{privacy_report()}} for the realized accounting.
}
