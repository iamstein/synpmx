% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pca.R
\name{synpmx_pca_summarize}
\alias{synpmx_pca_summarize}
\title{Summarize a trial into the quantities a synthetic copy is built from}
\usage{
synpmx_pca_summarize(
  data,
  roles,
  seed = NULL,
  dose_term = c("factor", "log"),
  pca_variance = 0.9,
  n_components = NULL,
  min_column_patients = NULL,
  min_arm_patients = 3L
)
}
\arguments{
\item{data}{Source PMX event data.}

\item{roles}{Explicit column roles from \code{\link[=pmx_roles]{pmx_roles()}}, including
\code{nominal_time}.}

\item{seed}{Seed for the one random step in summarizing: censored values are
replaced by a draw inside the censoring region before the basis is fitted,
so that a column where most subjects sit at the assay limit describes the
patients rather than the assay. The boundary is reapplied at generation.}

\item{dose_term}{How dose enters the score model. \code{"factor"} gives each arm
its own mean score vector and its own residual covariance. \code{"log"}
regresses the scores on \code{log1p()} of the total dose, which spends one
coefficient rather than one mean per arm and extrapolates to doses the
study did not run, at the cost of assuming the dose-response is log-linear
and that every arm has the same between-subject spread. A lower limit of
quantification breaks both assumptions, which is why \code{"factor"} is the
default.}

\item{pca_variance}{Cumulative variance the retained components must reach.}

\item{n_components}{Number of components, overriding \code{pca_variance}.}

\item{min_column_patients}{Minimum distinct patients holding an observation
in a grid cell for that cell to be modelled. Defaults to the larger of 3
and a tenth of the cohort.}

\item{min_arm_patients}{Minimum patients in every arm. An arm below it has
no spread of its own to model, so its mean score vector and its covariance
would describe the one or two patients in it. The function refuses rather
than summarizing them; pool the arm, drop the column from \code{strata}, or
exclude those patients before calling.}
}
\value{
A \code{pmx_trial_summary}.
}
\description{
The only stage that reads patient data. Reduces each subject's trajectories
and baseline covariates to principal-component scores, models those scores
against the arm, and fits a dosing model and a visit model per arm. The
returned object holds nothing but summaries: no patient row survives it.
}
\details{
Run this, look at what it produced with \code{\link[=pca_report]{pca_report()}},
\code{\link[=pca_dosing]{pca_dosing()}}, \code{\link[=pca_visits]{pca_visits()}} and \code{\link[=pca_components]{pca_components()}}, then pass
it to \code{\link[=synpmx_pca_generate]{synpmx_pca_generate()}}. Generation reads the model and nothing else,
so what those four functions show is the whole of what the synthetic data was
built from.

\code{nominal_time} is required. The grid it names is the axis every feature sits
on, and dose rows and observation rows are placed on it together, so it is
the caller's statement about the protocol rather than something inferred from
recorded times.

No formal privacy claim is made.
}
\examples{
data <- pmx_simulated_fixture(60)
roles <- pmx_roles(
  id = "ID", time = "TIME", nominal_time = "NTIME", dv = "DV", amt = "AMT",
  evid = "EVID", cmt = "CMT", dvid = "DVID", mdv = "MDV"
)
trial_summary <- synpmx_pca_summarize(data, roles)
trial_summary
pca_report(trial_summary)
}
\seealso{
\code{\link[=synpmx_pca_generate]{synpmx_pca_generate()}}, \code{\link[=synpmx_pca]{synpmx_pca()}}, \code{\link[=pca_report]{pca_report()}}.
}
