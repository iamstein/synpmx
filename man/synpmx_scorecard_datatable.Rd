% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scorecard.R
\name{synpmx_scorecard_datatable}
\alias{synpmx_scorecard_datatable}
\title{A scorecard as a coloured HTML table}
\usage{
synpmx_scorecard_datatable(x, ...)
}
\arguments{
\item{x}{A \code{\link[=synpmx_scorecard]{synpmx_scorecard()}}.}

\item{...}{Passed to \code{DT::datatable()}. Paging is off and row numbers are
suppressed by default, since the whole card is meant to be read at once
and \code{check} already names each row.}
}
\value{
An \code{htmltools::tagList} holding the coloured card and the notes that
knitting one carries. Without \code{DT} installed, \code{x} invisibly, having
printed it.
}
\description{
Displays a \code{\link[=synpmx_scorecard]{synpmx_scorecard()}} as an interactive table with the verdicts
coloured: \code{"FAIL"} in bold red on a light red, \code{"review"} in bold orange on
a light orange, \code{"unavailable"} in muted grey, and \code{"pass"} left as ordinary
text. A card is five verdicts among thirty-odd rows of prose, and the rows
that need reading are the ones that have to be findable without reading all
of it.
}
\details{
\code{synpmx_scorecard()} computes the card and this displays it, so the object
is unchanged and can be subset, saved or printed as usual. The colouring is
the only thing added.

What is emitted is what knitting the card itself emits, with the colouring
added: the card, then the B5 rare-level detail where a study has any.

\code{DT} is a suggested package rather than a required one -- \code{synpmx} has no
hard dependencies -- so without it installed this says so and prints the
card in the console form instead. The verdicts are all still there; only the
colour is missing.

The same restriction applies as to the card itself. Rows reading \code{"source"}
or \code{"both"} were computed from real patient data, so an HTML file holding
the whole table belongs in the environment the source lives in.
}
\examples{
data <- pmx_simulated_fixture(30)
roles <- pmx_roles(
  id = "ID", time = "TIME", dv = "DV", amt = "AMT", evid = "EVID",
  cmt = "CMT", dvid = "DVID", covariates = "WT"
)
synthetic <- suppressWarnings(synpmx_avatar(data, roles, seed = 1))
synpmx_scorecard_datatable(synpmx_scorecard(data, synthetic, roles))
}
\seealso{
\code{\link[=synpmx_scorecard]{synpmx_scorecard()}}.
}
