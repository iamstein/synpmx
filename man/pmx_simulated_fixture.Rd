% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fixtures.R
\name{pmx_simulated_fixture}
\alias{pmx_simulated_fixture}
\title{Fully simulated public repeated-dose fixture}
\usage{
pmx_simulated_fixture(n_subjects = 60L)
}
\arguments{
\item{n_subjects}{Positive number of fully simulated subjects. The default
is 60 to provide a larger companion to six- and twelve-subject examples.}
}
\value{
A deterministic data frame with two doses per subject, dose-relative
\code{cp}, global study-time \code{pd}, explicit nominal/TAD/occasion roles, and
fixed public factor levels.
}
\description{
Creates a deterministic two-endpoint PMX event table for privacy-utility and
workflow tests. The data are generated from fixed formulas and contain no
patient or proprietary information, so no random seed is required.
}
\details{
\code{TIME}, \code{NTIME}, and \code{TAD} are in hours; \code{AMT} is in arbitrary dose units;
\code{WT} is in kg; \code{AGE} is in years; \code{cp} uses arbitrary concentration units;
and \code{pd} uses arbitrary response units.
}
\examples{
public_data <- pmx_simulated_fixture(12)
length(unique(public_data$ID))
table(public_data$DVID, public_data$EVID == 0)
}
