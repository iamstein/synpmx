% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synpmx.R
\name{synpmx_calibrated}
\alias{synpmx_calibrated}
\title{Generate a dataset from a privately calibrated structural model}
\usage{
synpmx_calibrated(
  data,
  roles,
  model,
  design,
  priors,
  epsilon,
  n_subjects = NULL,
  seed = 123,
  n_datasets = 1L,
  covariates = NULL,
  backend = "opendp",
  public_source = FALSE
)
}
\arguments{
\item{data}{The confidential dataset.}

\item{roles}{A \code{\link[=pmx_roles]{pmx_roles()}} declaration for \code{data}.}

\item{model}{A public \code{\link[=pmx_structural_model]{pmx_structural_model()}}.}

\item{design}{A public \code{\link[=pmx_trial_design]{pmx_trial_design()}}.}

\item{priors}{A \code{\link[=pmx_priors]{pmx_priors()}} giving a public range per released correction.}

\item{epsilon}{The privacy budget. A governance decision, not a default;
\code{\link[=pmx_preflight]{pmx_preflight()}} reports the expected fold-error before any is spent.}

\item{n_subjects}{Number of subjects to generate. Defaults to the released
noisy count.}

\item{seed}{Ordinary generation seed. Unrelated to privacy noise, which is
controlled by the backend and is never user-seeded.}

\item{n_datasets}{Number of datasets to draw from the single release. One
dataset is returned directly; several are returned as a list.}

\item{covariates}{Optional \code{\link[=pmx_covariates]{pmx_covariates()}}.}

\item{backend}{Privacy backend. Defaults to the validated OpenDP adapter and
fails closed if it is unavailable.}

\item{public_source}{Assert that \code{data} is genuinely public. Required by, and
only meaningful for, \code{backend = "public"}, which makes no DP claim.}
}
\value{
A data frame in the generated event-table schema, carrying its
release so that \code{\link[=privacy_report]{privacy_report()}} and \code{\link[=synpmx_generate]{synpmx_generate()}} can read it.
A list of such data frames when \code{n_datasets > 1}.
}
\description{
Keeps a public structural model's \emph{shape} and spends a small privacy budget
correcting only its \emph{magnitude}. Each subject is reduced to a bounded
multiplicative correction of the model's own prediction, clipped to a public
prior range, and released with calibrated noise. Only a handful of numbers
leave the data, which is why this mode stays usable at 20 to 60 subjects.
}
\details{
This is the recommended differentially private path for early-phase cohorts.
The tradeoff is that everything not calibrated is \emph{asserted}: curve shape,
variability, residual error, and covariate relationships come from the public
model, so the output is only as realistic as that model.

\strong{This function spends privacy budget.} Calling it again spends the budget
again. To draw further datasets from the release you have already paid for,
use \code{\link[=synpmx_generate]{synpmx_generate()}} or ask for several at once with \code{n_datasets}.
}
\section{Maintenance status}{

A secondary, provided-as-is path. \code{\link[=synpmx_avatar]{synpmx_avatar()}} is the primary, maintained
method. The differentially private modes are complete and tested but not under
active development, carry known open findings, and have not been independently
privacy-audited. Use them to demonstrate the privacy/utility tradeoff, not as
a production release mechanism; a real regulated release needs specialist
review and the external OpenDP backend. Requires a session-level
acknowledgment: call \code{\link[=synpmx_enable_dp_engines]{synpmx_enable_dp_engines()}} once before this function
will run (unless \code{backend = "public"}, which makes no DP claim).
}

\seealso{
\code{\link[=synpmx_generate]{synpmx_generate()}} to draw more datasets for free,
\code{\link[=privacy_report]{privacy_report()}} for the realized accounting.
}
