% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{compare_pmx_rare_levels}
\alias{compare_pmx_rare_levels}
\title{Which rare source levels reached the synthetic output}
\usage{
compare_pmx_rare_levels(source, synthetic, roles, floor = NULL)
}
\arguments{
\item{source}{Source PMX data.}

\item{synthetic}{Generated synthetic PMX data. When it carries a
\code{"pmx_settings"} attribute, its \code{min_pattern_share} is used as the floor.}

\item{roles}{Explicit roles from \code{\link[=pmx_roles]{pmx_roles()}}.}

\item{floor}{Levels held by fewer than this many source patients are
\code{exposed}. Left \code{NULL} it is taken from the run, and \code{2} otherwise -- the
lowest value that means "more than one real patient".}
}
\value{
A \code{pmx_rare_levels} data frame, one row per categorical column and
level, with \code{source_patients}, \code{synthetic_patients}, \code{exposed} and
\code{reached}. Zero rows when the roles declare no categorical axis.
}
\description{
Censuses every categorical axis -- \code{strata} and each non-numeric covariate --
on both sides, and marks the levels \strong{too few source patients held} to be
safely copied out. That floor is \code{min_pattern_share}, the same rule the
generator applies to visit sets and the scorecard applies to copied vectors,
so no new threshold is introduced.
}
\details{
Read the \code{exposed} rows, and among them the ones that \code{reached} the output. A
level held by two real patients, appearing in a released table, says that
someone with that attribute was in this study; for a named trial with public
inclusion criteria that can be close to identifying on its own, and no cohort
size helps. The remedies are upstream of generation: drop the covariate from
\code{covariates}, or collapse its rare levels before generating.

This reads real patient data on both sides and is marked
\code{"restricted_not_releasable"}.

\strong{What it cannot see.} Rarity \emph{in the world}. If every living carrier of a
mutation is in this study, the source count is the whole population and looks
unremarkable. And it censuses each column on its own: with \code{d} covariates
there are \code{2^d} combinations that could single a patient out, and enumerating
them is not something this does.
}
\examples{
data <- pmx_simulated_fixture(20)
roles <- pmx_roles(
  id = "ID", time = "TIME", dv = "DV", amt = "AMT", evid = "EVID",
  cmt = "CMT", dvid = "DVID", covariates = c("WT", "SEX")
)
synthetic <- suppressWarnings(synpmx_avatar(data, roles, seed = 1))
compare_pmx_rare_levels(data, synthetic, roles)
}
\seealso{
\code{\link[=synpmx_scorecard]{synpmx_scorecard()}}, which reports this as row B5,
\code{\link[=compare_pmx_distributions]{compare_pmx_distributions()}},
\code{vignette("scorecard-synthetic-data-checks")}.
}
