on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  release:
    types: [published]
  workflow_dispatch:
  # Cache warming, not a build anyone is waiting for. Two thirds of this
  # workflow is `setup-r-dependencies` installing the tree before a vignette is
  # touched -- 17 of the 25 minutes on the 2026-08-19 run -- and that step is
  # only fast when its cache survives. GitHub evicts a cache 7 days after it was
  # last READ, so a weekly run races the window and loses whenever it fires a few
  # hours late; the 2026-08-26 push found no cache at all, exactly 7 days after
  # the previous run. Monday and Thursday leaves a maximum gap of four days.
  #
  # Reading the cache is what resets the clock, so these runs need do nothing
  # else. They do not deploy -- see the condition on the deploy step -- and a
  # failure on one is a genuine signal, since nothing in the repository changed
  # and something upstream must have.
  schedule:
    - cron: '0 6 * * 1,4'

name: pkgdown

jobs:
  pkgdown:
    runs-on: ubuntu-latest
    # Only restrict concurrency for non-PR jobs
    concurrency:
      group: pkgdown-${{ github.event_name != 'pull_request' || github.run_id }}
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: r-lib/actions/setup-pandoc@v2

      - uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true
          # pak does not read Additional_repositories from DESCRIPTION, so the
          # OpenDP r-universe has to be declared here or the suggested `opendp`
          # dependency cannot be resolved and the whole solve fails.
          extra-repositories: https://opendp.r-universe.dev

      # Install the hard dependencies plus exactly the suggested packages the
      # documentation actually executes, rather than every Suggests.
      #
      # A package missing from this list does not always fail the build, which
      # is what makes the list worth keeping in step with the documentation by
      # hand. `patchwork` missing stopped the site cold, because
      # `compare_pmx_distributions()` refuses to draw without it. `DT` and
      # `htmltools` missing did not: `synpmx_scorecard_datatable()` degrades to
      # the console form on purpose, so every card on the site rendered as an
      # uncoloured dump under a "DT is not installed" message and the build went
      # green. Adding a suggested package that any vignette or article executes
      # belongs in the same commit as the code that starts executing it.
      #
      # rxode2 is deliberately absent. Nothing in the documentation runs it:
      # the one rxode2 block, in vignettes/articles/model-elicitation.Rmd, is a
      # plain ```r fence that the article itself notes was never executed.
      # Installing it anyway pulled in rxode2 -> qs -> stringfish, whose TBB
      # symbols do not resolve against RcppParallel 6.0.0 ("undefined symbol:
      # ...concurrent_vector_base_v3..."), and downlit loaded it while
      # autolinking that article's rxode2 references. That failed every site
      # build from 2026-07-23 on. Without rxode2 installed, downlit leaves
      # those references as plain text and the block still renders.
      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          dependencies: '"hard"'
          extra-packages: |
            any::pkgdown
            any::DT
            any::ggplot2
            any::htmltools
            any::knitr
            any::nlmixr2data
            any::opendp
            any::patchwork
            any::rmarkdown
            any::xgxr
            local::.
          needs: website

      - name: Build site
        run: pkgdown::build_site_github_pages(new_process = FALSE, install = FALSE)
        shell: Rscript {0}

      - name: Prune agent-facing files from the site
        run: Rscript pkgdown/prune-site.R

      # Scheduled runs stop here. They exist to touch the dependency cache, and
      # redeploying byte-identical content twice a week would put a commit on
      # `gh-pages` for nothing.
      - name: Deploy to GitHub pages
        if: github.event_name != 'pull_request' && github.event_name != 'schedule'
        uses: JamesIves/github-pages-deploy-action@v4.5.0
        with:
          clean: false
          branch: gh-pages
          folder: docs
